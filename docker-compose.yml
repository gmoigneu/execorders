version: '3.8'

services:
  postgres:
    image: postgres:16
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: execorders
      POSTGRES_PASSWORD: execorders
      POSTGRES_DB: execorders